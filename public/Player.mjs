class Player {
  constructor({x, y, score, id}) {
    this.x = x;
    this.y = y;
    this.score = 0;
    this.id = id;
  }

  movePlayer(dir, speed) {

  }

  collision(item) {

  }

  calculateRank(arr) {

  }
}

export default Player;
